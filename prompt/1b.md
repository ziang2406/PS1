```markdown
# Problem 1b

## Prompt

For question 1b, I have already written down some codes for data loading and definitions in Q1.R file. Please retain the code and implement new code following my instructions.

Create an outer loop over horizons H = 1,...,20 in years. For each horizon H, the outer loop needs to identify the starting month with H years of future data available (meaning that t = 1,...,n-12*H). The outer loop also defines the current predictor dp_t. 

Within each horizon H, create inner loops over h = 1,...,H and construct the following regressors: 

sum_re = sum_{h=1}^H kappa^(h-1) * re_{t+h}

sum_dg =  sum_{h=1}^H kappa^(h-1) * dg_{t+h}

sum_dp = kappa^(H) * dp_{t+H}

Since the data is monthly, select observations at 12-month increments. 

For each H, the outer loop needs to use lm() function to run three OLS regressions of sum_re, sum_dg, and sum_dp on dp_t and stores the slopes as b_re, b_dg, and b_dp. 

Please create a line graph plotting b_re, b_dg, and b_dp against H horizons and save it in figures/1b_figure.png




## Notes


```