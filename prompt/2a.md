```markdown 
# Question 2a
## Prompt 
Let's work on question 2a using the EQ Dataset.csv. Note that since the data set contains monthly values, t+1 represents 12 months after t. Thus, move forward 12*h rows from starting month t to retrieve observations at t+h. Make sure that only use starting month t such that there are H years of future data available. 

For each horizon H = 1,...,15, calculate the following variables:
xR_{e,t+h} = exp(re_{t+h}) - exp(rf_{t+h})
D_t/P_t = exp(dp_{t})

Then, for each horizon H = 1,...,15, estimate the OLS regression using lm() regression with an intercept:

1/H * sum from h=1 to H of xR_{e,t+h} = alpha^{H} + beta^{H} * D_t/P_t + e_{t}^{H}

Print the estimated alpha^{H}, beta^{H}, and adjusted R-squared for each H. Plot the graph of adjusted R-squared against H horizons using the same consistent format of the previous prompts, storing the graph as figures/2a.png. The output should be code/2a.R, code/2a.csv, and figures/2a. 




```