```markdown 
# Question 2b
## Prompt 
Let's work on question 2b. Note that since the data set contains monthly values, t+1 represents 12 months after t. We calculate the following variables:
xR_{e,t+1} = exp(re_{t+1}) - exp(rf_{t+1})
D_t/P_t = exp(dp_{t}) 

Regressing the one-year future excess return on the current dividend-price ration using an OLS regression as follows: 
xR_{e,t+1} = a + b * D_t/P_t + e_{t+1}

Estimate the b coefficient (\hat_{b}) as well as corresponding standard error and t-stat using 5 following methods:
1. The baseline OLS standard error that doesn't account for heteroskedasticity and autocorrelation.
2. The White (1980) standard error that accounts for heteroskedasticity, but not for autocorrelation. 
3. The Newey and West (1987) standard error that accounts for heteroskedasticity and autocorrelation up to 11 lags.
4. The Hansen and Hodrick (1980) standard error that accounts for heteroskedasticity and autocorrelation up to 11 lags.
5. The Newey and West (1987, 1994) standard error that allows automatic lag selection and accounts for heteroskedasticity and autocorrelation. 

t-stat  = \hat_{b} / standard error 

Please print the results for \hat_{b} and its corresponding standard error (Std. Error) and t-stat as a LaText-formatted table and save it as figures\2b.tex. Output should be code\2b.R, code\2b.csv.

```